message("Build bindings")
find_package(Java)
if(Java_FOUND)
	message("Found Java")
	find_package(JNI)
	if(JNI_FOUND)
		message("Found JNI")
		ADD_SUBDIRECTORY( Java )
	endif()
endif()

find_package(Python)
if(Python_FOUND)
	message("Found Python")
	ADD_SUBDIRECTORY( Python )
endif()
